{
  "npmClient": "yarn",
  "packages": [
    "packages/*",
    "examples/*"
  ],
  "version": "independent",
  "useWorkspaces": true,
  "scripts": {
    "bump:version": "lerna version --no-push"
  }
}
