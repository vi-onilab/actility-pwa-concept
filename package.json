{
  "name": "pwa-concept",
  "version": "0.0.0",
  "author": "Onilab Team <talkto@onilab.com>",
  "private": true,
  "scripts": {
    "start": "yarn workspaces foreach run start",
    "build": "yarn workspaces foreach run build",
    "analyze": "yarn workspaces foreach run analyze",
    "clean": "yarn workspaces foreach exec 'rm -rf ./node_modules; rm -rf node_modules; rm -rf ./dist'; yarn install",
    "prepublish": "lerna run prepublish"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.16.7",
    "@babel/preset-react": "^7.16.7",
    "@rollup/plugin-commonjs": "^21.0.1",
    "@rollup/plugin-node-resolve": "^13.1.2",
    "@rollup/plugin-typescript": "^8.3.0",
    "rollup": "^2.63.0",
    "rollup-plugin-babel": "^4.4.0",
    "rollup-plugin-dts": "^4.1.0",
    "rollup-plugin-peer-deps-external": "^2.2.4"
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@3.1.1"
}
