import { module } from '@pwa-concept/core'
import graphqlSchemas from './graphql/schemas'
import graphqlResolvers from './graphql/resolvers'

const {{pascalCase name}}Module = module(() => ({
    graphqlSchemas,
    graphqlResolvers,
}))

export default {{pascalCase name}}Module
