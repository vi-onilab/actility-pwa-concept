extend type Query {
    cms(id: ID): Cms
}

type Cms {
    id: ID
    title: String
    content: String

    layout: String

    # Meta
    metaDescription: String
    metaKeywords: String
    metaTitle: String
}
