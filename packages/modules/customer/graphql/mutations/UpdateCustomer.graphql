mutation UpdateCustomer($input: CustomerUpdateInput) {
    updateCustomer(input: $input) @client {
        id
        firstName
        lastName
        email
        middleName
        dateOfBirth
        gender
        isSubscribed
    }
}
