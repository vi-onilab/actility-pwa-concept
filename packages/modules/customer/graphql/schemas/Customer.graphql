extend type Query {
    customer(token: String): CustomerOutput
}

type CustomerOutput {
    firstName: String
    lastName: String
    email: String
    middleName: String
    dateOfBirth: Int
    gender: Int
    isSubscribed: Boolean
}
