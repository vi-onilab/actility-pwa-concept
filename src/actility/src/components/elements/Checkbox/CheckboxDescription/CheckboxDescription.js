import { Icon } from 'components/elements/Icon';

import css from './CheckboxDescription.module.scss';

const CheckboxDescription = () => (
	<div className={css.checkboxDescription}>
		<Icon name="questionIcon" />
	</div>
);

export default CheckboxDescription;
