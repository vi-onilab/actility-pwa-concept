#import "../fragments/CartData.graphql"

mutation RemoveItemFromCart(
    $input: RemoveItemFromCartInput!
) {
    removeItemFromCart(
        input: $input
    ) {
        cart {
            ... CartDataFragment
        }
    }
}