#import "../fragments/WishListData.graphql"

mutation RemoveProductsFromWishList(
    $id: ID!
    $itemsIds: [ID!]!
) {
    removeProductsFromWishlist(
        wishlistId: $id
        wishlistItemsIds: $itemsIds
    ) {
        wishlist {
            ... WishListData
        }
    }
}