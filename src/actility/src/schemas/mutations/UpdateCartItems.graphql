#import "../fragments/CartData.graphql"

mutation UpdateCartItems(
    $input: UpdateCartItemsInput!
) {
    updateCartItems(
        input: $input
    ) {
        cart {
            ... CartDataFragment
        }
    }
}