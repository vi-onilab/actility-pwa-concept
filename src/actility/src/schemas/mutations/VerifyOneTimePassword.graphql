mutation VerifyOneTimePassword(
    $code: String!
    $email: String!
) {
    verifyOneTimePassword(
        code: $code
        email: $email
    ) {
        attemptsLeft: attempts_left
        token
        valid
    }
}
