#import "../fragments/CartData.graphql"

query Cart(
    $cartId: String!
    $isFull: Boolean = false
) {
    cart(
        cart_id: $cartId
    ) {
        id @skip(if: $isFull)
        totalQuantity: total_quantity @skip(if: $isFull)

        ... CartDataFragment @include(if: $isFull)
    }
}