query Countries {
    countries {
        available_regions {
            id
            code
            name
        }
        full_name_english
        full_name_locale
        id
        three_letter_abbreviation
        two_letter_abbreviation
    }
}