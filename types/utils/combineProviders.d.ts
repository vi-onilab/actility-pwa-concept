import { ReactElement } from 'react';
import { ModuleProvider } from '../types';
declare const combineProviders: (element: ReactElement, providers?: ModuleProvider[] | undefined) => ReactElement;
export default combineProviders;
